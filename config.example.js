/* Template for config.js — copy this file to config.js and fill in your values.
 *
 * Get them from: console.firebase.google.com
 *   → Project settings → Your apps → Web app → Config
 *
 * config.js is gitignored. On Netlify it is generated automatically from
 * environment variables by generate-config.js, so you do not upload it.
 */
window.GHARSEVA_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
